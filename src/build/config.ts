import { join } from "node:path"

export const buildDir = ".soar"
export const outDir = join(buildDir, "out")
export const rootDir = "."
export const pagesDir = join(rootDir, "pages")

export const distDir = "dist"
